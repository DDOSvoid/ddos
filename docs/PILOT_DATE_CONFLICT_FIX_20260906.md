# 2026-09-06 日期冲突隔离及无进展重试保护

## 证据与处置

公告 AN202304031585042912（数据库 ID 284303）的原始列表响应 notice_date 为
2023-04-04，数据库 published_date 同为 2023-04-04；已缓存正文接口 notice_date 为
2023-04-03。列表 eiTime 与正文 eitime 均指向 2023-04-03 17:56:44，但这不能证明
应该覆盖哪个公告日期。未修改公告日期、冻结计划、标签或来源校验；该公告仍未成功归档。

缓存证据位于试点 content_page_cache 下：
`cd8b1149b2ca36ae8de4bacd6154a2b7de4b195b99410d4fc5d364536bb5d565/`
`dacd98f1dfbb8cb782a959ad200e80e7b95a6a4f6fbd1de94b214f8bc774bd42/0001.json`。

## 修复行为

- 归档器仅将确切的日期冲突 ValueError 分类为不可自动重试；历史同类失败也适用。
  未删除失败记录，不将失败记为成功，不将未知 ValueError 一概隔离。
- 查询排除已记录的日期冲突，游标继续扫描后续候选。只有候选已耗尽且剩余失败均为
  不可重试时，队列将该阶段设为 needs_review，继续独立的数据下载阶段。
- needs_review 阶段重启后不会自动重试，必须在人工核实、修正来源并明确解除隔离后
  才恢复。当前日期冲突仍待核查，不得作为正式正文专家合格输入。
- 若其他归档错误导致连续三轮无新增、无已有归档确认，则标记 blocked 并退出，
  不再无上限循环。计数保存在队列状态中；有真实进展才重置。
- 空或缺字段报告不会被当作完成。所有任务结束但仍存在 needs_review 时，整体状态
  为 completed_with_exceptions，不是完整验收通过。

12 项测试通过，包括真实数据库夹具中“日期冲突只请求一次、后续合格公告正常归档、
失败保持未完成”，以及隔离跨重启、三轮停止、坏报告拒绝、缓存与 2026 封存检查。

## 运行交接

身份核验后关闭旧试点队列 PID 22904 及子进程，15:44 以单下载任务恢复，启动 PID 33900。
原状态与归档保留。新日志为：

- `logs/electrical_equipment_pilot_queue.failure_policy1.stdout.log`
- `logs/electrical_equipment_pilot_queue.failure_policy1.stderr.log`

2026 未解封。此修复只处理下载控制及异常记录，不表示正式三专家或融合已经通过验证。
